// Vamos explorar atribuições composta em operações matemáticas! Imprima o valor de cada uma das operações a seguir:

const saldo = 50;

const saldoComBonus = saldo + 20;

const saldoComDesconto = saldoComBonus / 0.5;

const saldoFinal = saldoComDesconto;


// Impessão na tela
console.log(`saldoInicial = ${saldo}`)

console.log(`saldoComBonus = ${saldoComBonus}`)

console.log(`saldoComDesconto = ${saldoComDesconto}`)

console.log(`saldoFinal = ${saldoComDesconto}`)

